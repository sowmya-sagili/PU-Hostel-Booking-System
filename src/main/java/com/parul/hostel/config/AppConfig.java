package com.parul.hostel.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // Placeholder for application configurations (e.g. CORS config, Security config)
}
